import { createTheme } from "@mui/material";

export const appMaterialTheme = createTheme({
    palette: {
        mode: "dark",
        primary: {
            main: "#ff8a00",
        },
    },
    typography: {
        fontFamily: "Inter, sans-serif",
    },
});
